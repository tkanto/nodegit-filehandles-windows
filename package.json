{
  "name": "nodegit-test",
  "version": "1.0.0",
  "description": "Simple test to demonstrate nodegit keeps .git/objects/pack/* files opened indefinitely",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Teemu Kantoluoto",
  "license": "ISC",
  "dependencies": {
    "fs-extra": "^0.30.0",
    "nodegit": "^0.16.0"
  }
}
